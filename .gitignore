# transient / machine-local — never commit
.DS_Store
**/.DS_Store

# python
**/venv/
**/__pycache__/
*.pyc

# pipeline runtime artifacts (regenerated locally, not needed in repo)
**/logs/
pipeline/tools/venv/

# transient state / exports (per-machine, not portable)
_catalysts_seen.json
held_export.txt

# local Claude Code session/permission state
.claude/settings.local.json
