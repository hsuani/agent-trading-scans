# Python deps for the trading-scan helper tools (render_html / build_dashboard /
# extract_catalysts / ta / yf). Cloud agent: `pip install -r requirements.txt`.
yfinance==1.3.0
pandas==3.0.3
stockstats==0.6.8
markdown==3.10.2
